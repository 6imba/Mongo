# create .env file in root dir:
    PORT = 8010
    DATABASE_URL = mongodb://localhost:27017

> npm i dotenv
> import dotenv from 'dotenv'
> dotenv.config()
> const PORT = process.env.PORT || 8000;
> const DATABASE_URL = process.env.DATABASE_URL

----------------------------------------------------------------------------------------------------------------

- keep apps secret data in .env file.
- this file is not placed in server.
- we .gitignore, .env file when pushing in github.

- port number
- db name
- token_secret_key
- db url

----------------------------------------------------------------------------------------------------------------

